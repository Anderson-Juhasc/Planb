require.config({
    paths: {
        jquery: '../bower_components/jquery/jquery'
    },
    shim: {
        //scrollable: {
        //    deps: ['jquery']
        //}
    }
});

require(['jquery'], function ($) {
    'use strict';
    // use app here

});
